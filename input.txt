function f(e:int): number {
  return (e as number);
}

// Main program
function main(): void {
  i,a,j:int;
  v:[10]number;
  
  a = 0;
  i = 0;
  j = -i*a-2;  
  v[(v[j] as int)] = (a as double);
  log 'f', '(', 'i', ')', '=' ,f(i);
}
